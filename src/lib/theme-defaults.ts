/**
 * Default accent = :root --primary in index.css (pale chartreuse #84BC00).
 * Hex form for persisted primary / color picker.
 */
export const DEFAULT_THEME_PRIMARY_HEX = "#84BC00";

/** Previous default emerald; migrated from localStorage to DEFAULT_THEME_PRIMARY_HEX. */
export const LEGACY_EMERALD_PRIMARY_HEX = "#157a58";

/** Deep emerald sometimes stored or sampled; migrated to DEFAULT_THEME_PRIMARY_HEX. */
export const LEGACY_DEEP_EMERALD_HEX = "#136f4f";

/** Previous neon default; migrated from localStorage when present. */
export const LEGACY_NEON_PRIMARY_HEX = "#00ff80";

/** Previous default; migrated from localStorage so UI matches integrations green. */
export const LEGACY_THEME_PRIMARY_HEX = "#85a506";
