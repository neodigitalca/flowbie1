import type { OverviewBulkActionCluster } from "@/lib/overview/overview-bulk-action-clusters";

/** Compression / merge-ratio toolbar removed — GSC AI triage drives keep vs consolidate. */
export function buildSitemapOptimizerToolbarClusters(): OverviewBulkActionCluster[] {
  return [];
}
