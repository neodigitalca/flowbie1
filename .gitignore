# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.local
.env.*.local

# WP Engine deploy config (contains credentials)
wordpress-plugins/flowbie-wpengine.config.json
wordpress-plugins/Customer List/SFTP Users_Clients List.csv

# Local credential overrides (never commit)
gsc-config.local.js
*-credentials*.json
*.credentials.json

# Generated WP plugin credentials (may contain keys)
wordpress-plugins/flowbie-wp/.env
wordpress-plugins/flowbie-wp/includes/flowbie-wp-secrets.php
wordpress-plugins/flowbie-wp/includes/flowbie-wp-gsc-config.php
wordpress-plugins/flowbie-app/includes/flowbie-app-secrets.php

# Deploy build artifacts (may embed local secrets)
wordpress-plugins/.deploy/*.zip
wordpress-plugins/*.zip

# Report date range (AI agents read this; per-user local state)
REPORT_DATE_RANGE.md

# Windows reserved-name junk file sometimes created in repo root
NUL

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
