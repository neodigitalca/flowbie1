<?php
/**
 * Optional wp-config overrides for NEO Pulse WP.
 *
 * Paste above "That's all, stop editing!" in wp-config.php.
 * Prefer plugin .env (see .env.example) or npm run embed:neo-pulse-wp-secrets before deploy.
 */

/** OpenRouter — AI wands, chat, search ranking */
// define( 'NEO_PULSE_WP_OPENROUTER_API_KEY', 'sk-or-v1-…' );
// define( 'NEO_PULSE_WP_OPENROUTER_MODEL', 'google/gemini-2.5-flash-lite' );
// define( 'NEO_PULSE_WP_OPENROUTER_HTTP_REFERER', 'https://neodigital.ca/neo-pulse-wp' );

/** SEO research — DataForSEO + Semrush */
// define( 'NEO_PULSE_WP_DATAFORSEO_LOGIN', 'your@login' );
// define( 'NEO_PULSE_WP_DATAFORSEO_PASSWORD', 'your-password' );
// define( 'NEO_PULSE_WP_SEMRUSH_API_KEY', 'your-semrush-key' );

/** AgentMail */
// define( 'NEO_PULSE_WP_AGENTMAIL_API_KEY', 'am_…' );
// define( 'NEO_PULSE_WP_AGENTMAIL_INBOX', 'you@agentmail.to' );

/** Optional GSC service account JSON for Analytics */
// define( 'NEO_PULSE_WP_GSC_SERVICE_ACCOUNT_JSON', '{"type":"service_account",…}' );
