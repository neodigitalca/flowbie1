<?php
/**
 * Optional wp-config overrides for Flowbie WP.
 *
 * Paste above "That's all, stop editing!" in wp-config.php.
 * Prefer plugin .env (see .env.example) or npm run embed:flowbie-wp-secrets before deploy.
 */

/** OpenRouter — AI wands, chat, search ranking */
// define( 'FLOWBIE_WP_OPENROUTER_API_KEY', 'sk-or-v1-…' );
// define( 'FLOWBIE_WP_OPENROUTER_MODEL', 'google/gemini-2.5-flash-lite' );
// define( 'FLOWBIE_WP_OPENROUTER_HTTP_REFERER', 'https://flowbie.ca/flowbie-wp' );

/** SEO research — DataForSEO + Semrush */
// define( 'FLOWBIE_WP_DATAFORSEO_LOGIN', 'your@login' );
// define( 'FLOWBIE_WP_DATAFORSEO_PASSWORD', 'your-password' );
// define( 'FLOWBIE_WP_SEMRUSH_API_KEY', 'your-semrush-key' );

/** AgentMail */
// define( 'FLOWBIE_WP_AGENTMAIL_API_KEY', 'am_…' );
// define( 'FLOWBIE_WP_AGENTMAIL_INBOX', 'you@agentmail.to' );

/** Optional GSC service account JSON for Analytics */
// define( 'FLOWBIE_WP_GSC_SERVICE_ACCOUNT_JSON', '{"type":"service_account",…}' );
